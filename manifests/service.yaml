apiVersion: v1
kind: Service
metadata:
  name: ciao-svc
  namespace: <namespace_name>
  labels:
    app.kubernetes.io/name: ciao
    app.kubernetes.io/version: 1.6.12
spec:
  type: ClusterIP
  ports:
    - port: 443
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app.kubernetes.io/name: ciao
    app.kubernetes.io/version: 1.6.12